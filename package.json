{
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.2",
    "fs-extra": "^11.2.0",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node_modules/ --base . -o . SimpleStorage.sol"
  }
}
